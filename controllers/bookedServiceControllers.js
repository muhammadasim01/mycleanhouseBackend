const bookedService = require("../models/BookedService");

const bookedServiceController = async (req, res) => {
  console.log("inside the bookedservices controllers");
};
module.exports = {
  bookedServiceController,
};
